1，其实没必要启用高级功能:-)

2，非要启用，按如下步骤，如果失败，从头再来：
	A，在系统中新建数据库库，名字为“phpmyadmin”，编码为utf8；
	B，将本文件夹中的“0phpmyadmin.sql”导入（恢复）到刚才建立的数据库中；
	C，修改本文件夹中的“config.inc.php”，将47行改为：“//*”，说明：
	   C1：47行原来是“/*”，
	   C2：修改完，也就是相当于启用了49到65行（原来被注释）。

	D，重新登录phpmyadmin。